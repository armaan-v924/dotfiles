# shell/utils

Drop reusable shell helper scripts here (functions, wrappers, etc.).

Files are automatically sourced by `shell/zsh/run_commands.sh` if they have a
`.sh` extension.

Keep scripts idempotent and side-effect free on load.
